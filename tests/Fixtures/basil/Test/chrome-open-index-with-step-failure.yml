config:
  browsers:
    - chrome
  url: http://nginx/index.html

"verify page is open":
  assertions:
    - $page.url is "http://nginx/index.html"

"fail on intentionally-missing element":
   assertions:
     - $".non-existent" exists
